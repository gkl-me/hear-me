<%- include('nav') %>

<div class="d-flex justify-content-center my-5">
    <div class="profile-container">
        <form action="" class="d-flex flex-column gap-3">
            <div class="d-flex gap-2">
                <label class="form-label" for="">Name</label>
                <input type="text" value="<%= user.name %>" class="form-input form-control">
            </div>
            <div class="d-flex gap-2">
                <label for="" class="form-label">Email</label>
                <input type="text" value="<%= user.email %>" class="form-input form-control">
            </div>
            <div class="d-flex gap-2">
                <label for="" class="form-label">Phone</label>
                <input type="text" value="<%= user.phone %>" class="form-input form-control">
            </div>
        </form>
    </div>
</div>
<div class="text-center"><a href="/user/logout" class="btn submit-btn mb-5">Logout</a></div>


<%- include('../admin/adminFooter') %>